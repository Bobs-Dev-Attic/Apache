/**
 * Single source of truth for the game version.
 * Bump this on every update (see CHANGELOG.md) — the loading screen and the
 * document title read from it automatically.
 *
 * Scheme: semantic-ish MAJOR.MINOR.PATCH
 *   PATCH — tweaks, fixes, small polish
 *   MINOR — new features / content
 *   MAJOR — large / breaking overhauls
 */
export const VERSION = '0.2.2';
